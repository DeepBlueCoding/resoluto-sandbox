# Staging

The input/output plumbing between the host workspace and the conduit: stage a workspace in before a
run, collect and fetch declared output globs back out after it.

::: resoluto.sandbox.stage_inputs

::: resoluto.sandbox.put_dir

::: resoluto.sandbox.collect_outputs

::: resoluto.sandbox.fetch_outputs
